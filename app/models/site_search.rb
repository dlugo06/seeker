# class SiteSearch #Not inhereting from activerecord base, this does not need to interact with the database
#   # def self.search(term)
#   #   tracks = Track.where title: term
#   #   releases = Release.where title: term
#   #   artists = Artist.where name: term
#   #   tracks + releases + artists
#   # end
# end
